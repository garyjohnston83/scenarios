services:
  postgres:
    image: postgres:15
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: scenarios
      POSTGRES_USER: scenarios
      POSTGRES_PASSWORD: scenarios
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
